# Copy to .env.local and replace placeholders.
# `.env.local` is ignored by git.

OPENAI_API_KEY=replace_with_new_key

# Optional: if omitted, the app will try to derive this from ~/.codex/config.toml
OPENAI_RESPONSES_URL=https://apikey.soxio.me/openai/v1/responses

# Optional: if omitted, the app will try to derive these from ~/.codex/config.toml
OPENAI_PLANNER_MODEL=gpt-5.4
OPENAI_SYNTHESIS_MODEL=gpt-5.4
OPENAI_EVALUATOR_MODEL=gpt-5.4
OPENAI_DOCUMENT_MODEL=gpt-5.4
OPENAI_MULTIMODAL_DOCUMENT_MODEL=gpt-5.4
OPENAI_LAYOUT_MODEL=gpt-5.4
